# demo-domain policy

- All code under `packages/demo-domain/src/` follows layer rules in `policy/layers.yaml`.
- Cross-cutting concerns only via `providers/`.
- Specs for this domain: `docs/exec-plans/active/demo-domain/`.
