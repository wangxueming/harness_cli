# Per-role soul

- One `souls/<role>/SOUL.md` per role (~400 tokens target).
- Sections: Identity, Hard constraints, Escalation, Voice.
- **No** task steps (use `templates/mission/`).
- **No** layer rules (use `policy/`).

Validate: `pnpm harness validate-souls`
