# Subagents

- **Generated**: `.cursor/agents/*.md` from `pnpm agents:compile`
- **Invoke**: `/reviewer`, `/implementer`, or natural language
- **Built-in**: explore, bash, browser (Cursor)

SDD full prompts remain in `.claude/agents/sdd/` until P2 unify.
