# Model routing

Source: `config/models.registry.yaml`, `config/providers.yaml`.

Precedence: per-send override > subagent frontmatter > DAG complexity > role defaults.

P1: Cursor only. Enable anthropic/codex in P2.
