# Loop

1. Small steps with external verification each round.
2. State in `artifacts/progress/<domain>/<task>.md` (gitignored content OK).
3. **Done when** = commands in Spec (e.g. `pnpm harness check`).
4. Stop on red CI—fix before expanding scope.
